import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 7,
    borderRadius: 8,
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 600,
    color: '#d1d5db',
  },
})
